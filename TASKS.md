# Tasks — claude-ops-bot

Живая доска проекта. Эта карта — единственная, что читают сессии.
Завершённое уходит в DONE.md (его сессии НЕ читают — гигиена контекста).

Порядок Backlog = порядок выполнения (сверху вниз). Группы помечены жирным комментарием — это просто навигация, парсер их игнорирует.

> 2026-05-31: рефакторинг-проход закрыт — 25 карточек (security, рефактор бэк/фронт, OSS-доки, тесты 207→300) ушли в DONE.md. Карта кода → ARCHITECTURE.md. Ниже — только фичи/roadmap.

## Backlog
- [ ] Кстати такой момент - после того как я в ручную переименовал проект, пропали все сессии общения. И почему то мне CLaude <!--ops:ac3ff0-->
  > сказал что он сам не может переименовать проект. Короче надо решить вопрос вопрос создания нового проекта.
- [ ] Авто-создание forum-топика в Telegram при создании проекта в кокпите <!--ops:89f1cd-->
  > Сейчас «+ Новый проект» в кокпите создаёт папку/файлы и пишет в topics.json СИНТЕТИЧЕСКИЙ ключ chat:thread (напр. -100…:1780365319) — реального forum-топика в Telegram нет, чат/авто-запуск через TG для такого проекта не работают (см. артефакт untitled-1780365319).
  > 
  > Идея Игоря: на создании проекта авто-создавать топик в TG.
  > 
  > Реализуемо — бот @ziraclaudebot админ супергруппы с manage_topics:
  > 1. PTB: thread = await ptb_app.bot.create_forum_topic(chat_id=GROUP_CHAT_ID, name=project_name); tid = thread.message_thread_id.
  > 2. Писать в topics.json реальный ключ "<GROUP_CHAT_ID>:<tid>" → {project, cwd, model} вместо синтетического.
  > 3. (опц.) приветственное сообщение в новый топик + closeForumTopic при удалении проекта.
  > Эффект: проект, созданный в вебе, сразу появляется топиком в Telegram — готов к чату и авто-запуску карточек. Учесть: rollback при ошибке createForumTopic; дедуп если топик с таким именем уже есть.
- [ ] ⭐ Настройки проекта + глобальные настройки (UI): таб «Settings» в кокпите. <!--ops:f2ba02-->
  > Per-project: чекбокс «самолечение» (self_heal), log_cmd, test_cmd, notify_on_error, модель, видимость карточек, таймауты watchdog.
  > Глобальные: SELF_HEAL_ENABLED (master-kill), SELF_HEAL_MAX_CONCURRENT, SCAN_INTERVAL_SEC, дефолт-модель, дефолт-watchdog.
  > Сейчас всё правится руками в data/topics.json и env юнита — нужен UI с валидацией. Hot-reload topics.json уже работает (mtime-гейт в _maybe_reload_topics), глобальные — отдельный data/settings.json.
  > Контекст: возник 2026-06-02 при включении самолечения для claude-ops-bot — пришлось руками править topics.json.
- [ ] TG-канал: проверить, что контекст наполняется корректно и НЕ дублируется при каждом сообщении (была проблема в веб-версии — кэш дублировался и сбрасывался, контекст «съедался»); сравнить с веб-каналом и зафиксировать тестом <!--ops:9aa43f-->
- [ ] TG-канал: очередь сообщений во время работы агента — если второе сообщение приходит пока агент не закончил первое, оно должно вставать в очередь и обрабатываться после (как в веб-версии), а не отбиваться «уже работаю» <!--ops:b53401-->
- [ ] Хранение ключей: продумать и реализовать хранилище credentials — проектные ключи (API keys, tokens сервисов) + глобальные ключи; UI в кокпите; безопасное хранение; доступ агенту <!--ops:a7b2c1-->
- [ ] Модели + thinking mode: убрать дубль селектора (оставить в чате), добавить версии моделей и режимы мышления (max/min/default) <!--ops:4df23a-->
- [ ] multi-chat: несколько чатов на один проект, каждый со своим session_id; полоса вкладок чатов <!--ops:3a00f3-->
- [ ] Карточки: своя свежая сессия на карточку + cwd-замок (развести с общей сессией чата перед автономией) <!--ops:2a0a1a-->
- [ ] Единый Schedules: cron + systemd timers + /schedule + celery + n8n <!--ops:cb8518-->

## In Progress

## Review
- [?] Глобальные скиллы: показывать в кокпите + подготовить проект к публикации на GitHub (закрывать потребности разных пользователей) <!--ops:10f166-->

## Failed
- [!] Кнопка Стоп пропадает при переключении вкладки <!--ops:e46770-->
