# Production runtime dependencies for Cardloop.
# Python >= 3.11 required (developed and tested on 3.12).
# Dev/test deps live in requirements-dev.txt.
#
# Install:  python -m venv venv && venv/bin/pip install -r requirements.txt

# Claude Agent SDK — >=0.2.127 is REQUIRED: older SDKs silently downgrade newer
# model aliases without erroring. The bundled CLI determines alias resolution, so
# the floor must track model releases: <0.2.127 bundles CLI 2.1.191, which resolves
# "opus"->Opus 4.8 and "sonnet"->Sonnet 4.6 even though Opus 5 / Sonnet 5 exist.
# 0.2.127 bundles CLI 2.1.219, the first to resolve "opus"->Opus 5 (released 2026-07-24).
# 0.2.129 bundles CLI 2.1.221 (same as the operator's terminal CLI); no query.py delta vs
# 0.2.127 — bumped for bundle parity per the model-alias gotcha.
claude-agent-sdk>=0.2.129

# aiohttp cockpit (web server + SSE).
aiohttp==3.13.5

# Cron/interval scheduling for the autorun board.
APScheduler==3.11.2

# Encrypted secret store (Fernet) + auth crypto.
cryptography>=48

# Web Push (VAPID) — spec-053 Phase B browser push notifications.
pywebpush>=2.3

# .env loading.
python-dotenv>=1.2

# Structured concurrency primitives used by the SDK and engine.
anyio>=4.13
